**A pull request that does not fill this in is closed without review.** See
[CONTRIBUTING.md](https://github.com/kukv/octoscope/blob/main/CONTRIBUTING.md) before
opening one. Commits use a `feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:` prefix;
CI must pass.

## What and why

<!-- What changes, and why. "Why" is required — a diff without a reason is closed.
     Link the issue it resolves (`Closes #123`). -->

## Verification

<!-- The exact commands you ran and what they printed. `make check` is the minimum.
     For a change to the TUI, passing tests are not enough: say how you ran it
     (`go run ./cmd/octoscope ...`) and what you saw, including with `--lang ja` —
     full-width characters take two columns and break layouts that look fine in English.
     A screenshot is welcome. "I tested it" is not enough on its own. -->

## Scope

<!-- If you touched `internal/` or `cmd/`, name the plan under `docs/superpowers/plans/`
     this follows. If you touched a workflow, confirm any new or changed action is pinned
     to a full commit SHA with a `# vX.Y.Z` comment. -->

## Release notes label

<!-- Which label from .github/release.yaml applies, so the maintainer can attach it:
     Impact: Breaking, Kind: Feature, Kind: Enhancement, Kind: Bug Fix or Kind: Dependencies.
     The label also decides the version bump — see "Releases" in CONTRIBUTING.md. -->

## AI use (verified?)

<!-- If AI assisted this submission, confirm per
     [Use of AI](https://github.com/kukv/octoscope/blob/main/CONTRIBUTING.md#use-of-ai)
     that you've read, verified and can defend the output. -->
